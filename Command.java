/**
 * A interface with an execute method to be implemented by classes.
 * @author Ana Boccanfuso
 */
public interface Command {

    /**
     * An execute method that calls the MilitaryCadence class
     * to sing the cadences.
     */
    public void execute();
}